import React from "react";
import styles from "./Project.module.css";

function Project() {
  return (
    <section
      className={`${styles.jh_section} ${styles.jh_site_stats_section} ${styles.v1}`}
    >
      <div className={`${styles.container}`}>
        <div className={`${styles.jh_site_stats_inner}`}>
          <div className={`${styles.section_header}`}>
          
           
          </div>
          
        </div>
      </div>
    </section>
  );
}

export default Project;
